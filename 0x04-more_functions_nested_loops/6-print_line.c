#include "main.h"

/**
  * print_line - Draws a straight line according to parameter
  * @i: The number of lines to draw
  *
  * Return: empty
  */
void print_line(int i)
{
	int a;

	if a <= 0)
	{
		_putchar('\n');
	}
	else
	{
		for (a = 0; a < i; a++)
		{
			_putchar(95);
		}

		_putchar('\n');
	}
}
